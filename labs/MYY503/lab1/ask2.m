#        #
 # # # #
  #Lab1#
  # # # #
 #       #

##########################
# Askisi 2  - Dianismata #
##########################

# dianysma a
a = [2, 4, 8]

# dianysma b
b = [3, 2, 7]

# a. vres athroisma kai diafora

sum = a + b
sub = a - b

# b. na vrw ta anastrofa dianysmata

an_a = a'
an_b = b'

# c. eswteriko ginomeno twn a kai b'
eswt = a * b'

# d. ginomeno stoixeio pros stoixeio twn a kai b
ginomeno_stoixeiwn = a .* b

# e. neo dianisma c, na vrw max, min, size, length
c = [a, -b]

c_max = max(c)
c_min = min(c)
c_size = size(c)
c_length = length(c)

# st. w dianysma 20 thesewn gemato me 1 kai z idio alla me midenika
w = ones(1,20)
z = zeros(1,20)

# z. na ftiaksw ena dianysma k me stoixeia apo to 0 ews to 100
k = [0:0.1:100] # apo 0 ews 100 me vima 0.1
k = linspace(0,100,100) # apo 0 ews 100 me 100 stoixeia mono

# h. typwse ta stoixeia apo thesi 35 ews 50 sto k
k(35:50)
